import os
import pytest

# ⚠️ Adapte l'import à ton projet si besoin
# On suppose que tu as une factory create_app dans api/app.py
from api.app import create_app


@pytest.fixture(scope="session")
def app():
    """
    Application Flask pour les tests.
    - Passe l'app en mode TESTING
    - Permet d'injecter une config spéciale si besoin.
    """
    # Variables pour que ton code sache qu'on est en test
    os.environ.setdefault("FLASK_ENV", "testing")
    os.environ.setdefault("TESTING", "1")

    # Si tu veux une DB de test, configure ici :
    # os.environ.setdefault("DATABASE_URL", "sqlite:///:memory:")

    app = create_app(testing=True)

    # Optionnel : désactiver le CSRF, les logs verbeux, etc.
    app.config.setdefault("WTF_CSRF_ENABLED", False)

    yield app


@pytest.fixture()
def client(app):
    """
    Client HTTP Flask pour les tests (équivalent à requests mais en mémoire).
    """
    return app.test_client()


@pytest.fixture()
def runner(app):
    """
    Pour tester des commandes CLI Flask éventuellement.
    """
    return app.test_cli_runner()


@pytest.fixture(scope="session")
def api_base_path():
    """
    Préfixe des routes de ton API.
    Par défaut: /integrator/v1 (modifiable via API_BASE_PATH).
    """
    return os.getenv("API_BASE_PATH", "/integrator/v1")
